
name: glycplus
description: "Une application Flutter pour la gestion du diabète."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - Let pub resolve the correct compatible versions
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any

  # Other dependencies
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  google_fonts: ^6.2.1
  fl_chart: any
  intl: any
  flutter_speed_dial: ^7.0.0
  csv: ^6.0.0
  path_provider: ^2.1.5
  rxdart: ^0.28.0
  shared_preferences: ^2.5.3
  file_picker: ^8.0.7
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
